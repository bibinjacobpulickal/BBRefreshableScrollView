import XCTest
@testable import BBRefreshableScrollView

final class BBRefreshableScrollViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BBRefreshableScrollView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
